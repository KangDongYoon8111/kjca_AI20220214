using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class chosylove_Chapter4 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /*
         * 1. 답 : 후연산, 선연산 
         * 2. 답 : 2, 2, 4, 5
         * 3. 답 : ABC  */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
