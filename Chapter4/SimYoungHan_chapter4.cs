using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SimYoungHan_chapter4 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {


        // i++와 ++i 차이점은 무엇인가요?
        // 답: i++은 출력후 실행 ++i는 실행후 출력
        // int i = 1;
        // debug.log(i = i +1): 답: 2
        // debug.log(i++);      답: 2
        // debug.log(++i):      답: 4
        // debug.log(i +=1);    답: 5
        
        // int a = 10;
        //string b = a == 0 ? "가나다 :ABC";
        // 답: ABC

       

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
