using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KimTaeHun_Chapter3 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        string a = "30";
        string b = "40";
        int i = int.Parse(a);
        int j = int.Parse(b);
        Debug.Log(i*j);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
